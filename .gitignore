/target
# tools/package.py output, and the two copies ci.yml builds to compare
/dist
/dist-a
/dist-b
/rebuilt
/published
# `python3 -m compileall tools` in CI, and any local import of them
__pycache__/
*.pyc
